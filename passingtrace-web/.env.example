VITE_IDENTITY_AUTHORITY=https://localhost:56228
VITE_OIDC_CLIENT_ID=passingtrace-web
# Events API 地址（与 AppHost 编排分配或独立启动的 PassingTrace.Events.Api 一致）。
# 本地独立调试常见值：https://localhost:54933；通过 AppHost 编排时改为 AppHost 注入的地址。
VITE_EVENTS_API_BASE_URL=https://localhost:54933
